# Social Network build with golang

The goal is to learn golang

## Getting started
1. Install `go`

1. Install dependencies
    ```bash
    make init
    ```

1. Run web server
    ```bash
    make run
    ```
