# Social Network build with golang

The goal is to learn golang

## Getting started
1. Install `go`

1. Install dependencies
    ```bash
    go get ./...
    ```
1. Run web server
    ```bash
    go run main.go
    ```
